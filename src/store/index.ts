export { 
  initialState as markersInitialState,
  reducer as markersReducer,
} from './markersReducer';

export { 
  initialState as historiesInitialState,
  reducer as historiesReducer,
} from './historiesReducer';
